import type { Preview } from '@storybook/react';
import '../src/app/globals.css'; // Import global styles (Tailwind)

const preview: Preview = {
  parameters: {
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/i,
      },
    },
    nextjs: {
      appDirectory: true,
    },
  },
};

export default preview;